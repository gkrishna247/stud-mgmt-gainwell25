# General IDE/Editor files
.idea/        # IntelliJ IDEA
.vscode/      # VS Code
*.swp         # Vim swap files
*.swo
._*           # macOS hidden files

# Node.js
node_modules/  # Node.js modules (very important - never commit these!)
npm-debug.log* # npm debug logs
yarn-error.log  # Yarn error log
.pnp*         # Yarn Plug'n'Play files
.env          # Environment variables (e.g., API keys - ***CRUCIAL***)
.DS_Store


# Build/Distribution
dist/          # Output directory (if you build/bundle your code)
build/         # Another common build directory name

# Temporary Files/Cache
tmp/
temp/
*.tmp
*.bak        # Backup files
*.cache       # Cache files

# API Keys (Absolutely critical to exclude - examples)
# **Even better than a .gitignore entry for API keys is to use environment variables!**
#  Never commit files containing actual API keys, even as examples.
api_keys.json      # If you *must* have a file, NEVER commit real keys!
credentials.json   # Example:  Avoid names that hint at secrets.
config.js   # if config file store secret keys.
keys.js
*.key             # Don't commit raw key files.

# Other (adjust based on your tools/framework)
coverage/    # Code coverage reports
.nyc_output/ # More code coverage stuff
*.log        # General log files
.history/
public/

#Example data - don't add your real datasets to Git (usually too large!)
data/example/*

#---